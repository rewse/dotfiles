[default]
region = ap-northeast-1
output = yaml
{{- if eq .chezmoi.hostname "7cf34ded5d65" }}
credential_process = isengardcli credentials --role Admin --awscli 070392599442
{{- end }}

[hugo]
region = ap-northeast-1
output = yaml
aws_access_key_id = {{ onepasswordRead "op://chezmoi/Hugo - AWS IAM/username" }}
aws_secret_access_key = {{ onepasswordRead "op://chezmoi/Hugo - AWS IAM/credential" }}

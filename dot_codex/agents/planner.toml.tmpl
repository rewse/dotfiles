name = "planner"
description = {{ includeTemplate "agents/description/planner.txt" . | trim | toJson }}
sandbox_mode = "read-only"
developer_instructions = {{ includeTemplate "agents/body/planner.md" . | trim | toJson }}
